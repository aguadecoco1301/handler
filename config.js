let config = {
	token: "YOUR TOKEN HERE",
	events: {
		dir: "./events"
	},
	commands: {
		dir: "./commands"
	},
	lang: "en", //Default language
	prefix: "." //Default prefix
}

module.exports = config
